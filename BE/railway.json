{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan cache:clear && php artisan migrate --force && echo 'Starting serve on port '$PORT && php artisan serve --host=0.0.0.0 --port=${PORT:-8080} 2>&1",
    "restartPolicyType": "ON_FAILURE"
  }
}
